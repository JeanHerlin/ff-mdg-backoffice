import { ComingSoon } from "@/components/coming-soon";

export const metadata = { title: "Vérifications" };

export default function VerificationsPage() {
  return (
    <ComingSoon
      module="Module 4 — Vérification équipes & joueurs"
      title="File de vérification"
      description="Certifiez les équipes en attente et validez le pseudo des joueurs à partir de leur ID Free Fire."
    />
  );
}
