import { ComingSoon } from "@/components/coming-soon";

export const metadata = { title: "Statistiques & points" };

export default function StatsPage() {
  return (
    <ComingSoon
      module="Module 10 — Statistiques & points"
      title="Statistiques & points"
      description="Import Excel des résultats, calcul automatique des points et retrait manuel par un administrateur."
    />
  );
}
